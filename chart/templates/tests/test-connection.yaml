apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shellinabox.fullname" . }}-test-connection"
  labels:
    {{- include "shellinabox.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shellinabox.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
